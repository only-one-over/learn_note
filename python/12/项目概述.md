# 项目概述

这是一个实用的文件批量重命名工具，帮助用户高效管理大量文件。支持添加前缀/后缀、替换文本、添加时间戳、规范化命名等多种操作，适合整理照片、文档、日志文件等场景。

核心功能：

添加前缀：在文件名开头添加指定文本

添加后缀：在扩展名前添加文本（如 backup_file.txt）

替换文本：批量替换文件名中的特定字符串

添加时间戳：在文件名中添加当前日期时间

规范化命名：转换为小写、空格转下划线、去除特殊字符

预览功能：显示重命名前后的对比，避免误操作
